# 直播高光实时回放（REPLAY）自动录制系统 · 可行性文档

> 文档用途：供新开 agent 落地实现，本文档为需求界定 + 技术可行性评估 + 落地架构设计。
> 与「瓦电赛事运营中台（Jinx2）」的关系：本系统**独立部署**，中台不接入、不承载录制逻辑。中台后续可作为"高光素材展示/管理前端"对接本系统的产物（可选，本文档不展开）。

---

## 1. 需求重定义（基于澄清）

| 原始表述 | 澄清后含义 |
|---|---|
| "抓取三杀/四杀/五杀瞬间" | 直播画面中导播切出的**实时回放转场片段**，转场字不限于 REPLAY，还包括：`REPLAY`、`高效火力`（multikill）、`CLUTCH`、`ACE`、`THRIFTY` 等。识别对象 = 直播流画面上的转场字幕/横幅。 |
| "自动录制回合 REPLAY" | 录制的是**直播流里那段回放转场画面本身**（含前后若干秒），不是原生 `.rofl` 回放文件、不是观战 SDK 导出。 |
| "实时事件源只能依赖直播流" | 无 GSI、无官方/转播数据 API。**唯一信号源 = 直播视频流本身**，需通过画面识别（OCR/视觉）或音频特征判断"此刻是高光回放"。 |
| "按序号/地图/回合数/队伍/选手ID/连杀数命名" | 文件命名规则（见 §6），其中"地图/回合/选手ID/连杀数"**无法从直播流自动精确获取**，需人工标注或半自动推断（见 §7 风险）。 |
| "允许用户手动截取回放" | 提供手动触发：标记当前直播时间点 → 截取前后窗口 → 命名落库。复用自动录制的切片/命名/落库逻辑。 |
| "直播卡顿自动重录，且不阻塞新录制" | 主通道失效时有备用通道补录；录制任务必须**并发/队列化**，重录任务与新的高光录制互不影响。 |

### 不在本系统范围内（明确边界）
- 不接入 VCT 中台代码库（Jinx2）。
- 不获取原生 VALORANT 回合回放文件（`.rofl`）。
- 不做实时弹幕/语音解说处理（除非后续扩展）。
- 不负责素材的社媒分发（中台或人工处理）。

---

## 2. 总体可行性判定

| 子能力 | 可行性 | 说明 |
|---|---|---|
| 直播流接入（拉流/录制） | ✅ 成熟 | ffmpeg / streamlink 拉 HLS/RTMP，社区方案稳定 |
| 高光转场识别（画面） | ⚠️ 可行但需调参 | OCR + 模板匹配识别转场字，准确率受画质/语言/遮挡影响 |
| 精准切片（高光前后窗口） | ✅ 可行 | 识别到信号后 ffmpeg 切时间窗 |
| 文件命名规则 | ✅ 可行（部分字段需人工） | 纯字符串拼接，见 §6 |
| 手动截取 | ✅ 可行 | 触发逻辑简单，复用切片管线的后半段 |
| 卡顿自动重录 + 不阻塞 | ⚠️ 需后端架构支撑 | 需任务队列 + 并发 worker，非单脚本 |
| 全自动获取地图/回合/选手ID | ❌ 不可靠 | 直播画面无结构化数据，只能人工或半自动 |

**结论：技术上可行，核心难点是"直播流画面高光转场的稳定识别"和"并发录制架构"。整体属于一个独立的后端服务系统，不依赖中台。建议立项。**

---

## 3. 推荐技术架构

```
┌─────────────────────────────────────────────────────────┐
│  直播源（Twitch/YouTube/Bilibili HLS 或 本地采集卡）        │
└───────────────┬─────────────────────────────────────────┘
                │ 拉流 (streamlink / ffmpeg)
┌───────────────▼─────────────────────────────────────────┐
│  Ingest Worker（常驻）                                    │
│  - 持续拉流 → 写环形缓冲（rolling buffer，保留最近 N 分钟） │
│  - 同时抽帧送入识别管线                                    │
└───────────────┬─────────────────────────────────────────┘
                │ 帧序列
┌───────────────▼─────────────────────────────────────────┐
│  Detection Worker（可多实例）                             │
│  - OCR（中文/英文转场字）+ 模板匹配 + 可选轻量 CV          │
│  - 输出结构化事件：{type, ts_start, ts_end, confidence}   │
│  - 去抖/合并（同一高光多次命中只发一次）                   │
└───────────────┬─────────────────────────────────────────┘
                │ 事件入队
┌───────────────▼─────────────────────────────────────────┐
│  Task Queue（Redis / 内存队列）                           │
│  - 高光录制任务（主通道：从环形缓冲切实时片段）            │
│  - 补录任务（备用通道：从官方 VOD 定点重切）              │
│  - 手动截取任务（运营触发）                               │
└───────────────┬─────────────────────────────────────────┘
                │ 并发消费
┌───────────────▼─────────────────────────────────────────┐
│  Recorder Workers（多实例，互不阻塞）                     │
│  - 主通道：从 Ingest 环形缓冲按 ts 切出片段 → 命名 → 落库 │
│  - 备用通道：ffmpeg 从官方 VOD 按 ts 重切（延迟几分钟）   │
│  - 每个任务独立进程/协程，失败重试不影响其他任务          │
└───────────────┬─────────────────────────────────────────┘
                │
┌───────────────▼─────────────────────────────────────────┐
│  Storage（本地磁盘 / 对象存储）+ Metadata DB              │
│  - 片段文件 + 元数据（命名、时间戳、检测置信度、来源通道） │
└─────────────────────────────────────────────────────────┘
```

### 关键设计决策
1. **环形缓冲（rolling buffer）是核心**：Ingest 持续把直播流写入"最近 N 分钟"的缓冲（如 5 分钟循环文件）。检测到高光时，从缓冲里反向切出"命中前 X 秒 + 命中后 Y 秒"，无需录全流。
2. **主通道 + 备用通道双写**：主通道用环形缓冲实时切；卡顿/断流时主通道失败，任务自动转为"备用通道"——等官方 VOD 上线后按同一时间戳从 VOD 重切。VOD 是点播源，随时可重切，**天然不阻塞新高光**。
3. **任务队列 + 多 worker**：所有录制都是队列任务，Recorders 并发消费。重录任务和新高光任务只是队列里两条独立项，互相不阻塞。
4. **识别与录制解耦**：Detection 只产出"事件+时间戳"，Recorder 只管切片。识别挂了不影响已缓冲素材的手动/补录。

---

## 4. 高光转场识别方案（核心难点）

### 4.1 识别目标词表
- 英文：`REPLAY`、`ACE`、`CLUTCH`、`THRIFTY`、`MULTI KILL`、`QUADRA`/`QUAD`、`FLAWLESS`
- 中文（国服/中文流）：`回放`、`高效火力`、`残局`、`克敌`、`以一敌多`、`完胜`
- 注：不同赛事/解说流转场字不同，**词表需可配置**，首期以用户常看的流为准。

### 4.2 识别手段（按推荐顺序）
1. **模板匹配（首选，最稳）**：对已知转场画面截图做特征匹配（如转场 logo 位置、配色块）。转场画面通常样式固定，匹配率高、误报低。
2. **OCR（补充）**：对每帧跑轻量 OCR（Tesseract / PaddleOCR / 云端 OCR），命中词表即触发。处理中英文混排、艺术字体时需调参。
3. **音频特征（可选）**：高光常伴特定音效/解说语调，可作为辅助置信度，不建议作主通道。

### 4.3 去抖与合并
- 同一高光转场可能持续 3–8 秒、每帧都命中 → 用时间窗口合并（如 10s 内同类型命中归一个事件）。
- 输出事件带 `confidence`，低于阈值的进"待人工确认"而非直接录制。

### 4.4 准确率预期（坦诚）
- 模板匹配 + 固定词表：**85–95%** 召回，误报低。
- 纯 OCR 泛化：**60–80%** 召回，艺术字/遮挡易漏。
- **结论**：首期以模板匹配为主 + OCR 为辅，留人工确认通道兜底。做不到 100% 自动，需接受"漏切/误切可手动补/删"。

---

## 5. 卡顿自动重录设计（明确诉求）

| 场景 | 行为 |
|---|---|
| 直播流短暂卡顿（画面冻帧） | Ingest 检测冻帧/码率掉零 → 标记主通道异常；已缓冲片段仍可切；后续新高光照常入队 |
| 直播流中断（拉流失败） | 主通道任务失败 → 自动转备用通道（等 VOD） |
| 识别管线卡顿 | 不影响 Ingest 缓冲与 Recorder；识别恢复后继续；缓冲保留窗口内的素材仍可补切 |
| 备用重录进行中来了新高光 | 新高光是新队列任务，由另一个 Recorder worker 并发处理，**不与重录争资源/串行阻塞** |

**不阻塞的保证来自**：录制是队列任务 + 多 worker 并发，重录任务和高光任务在队列里平级，各自独立消费。

---

## 6. 文件命名规范

建议格式（字段用下划线分隔，便于解析）：
```
{序号}_{地图}_{回合}_{队伍ID}_{选手ID}_{连杀数}_{类型}_{来源通道}.mp4
```
示例：`003_ascent_R12_EDG_ZmjjKK_5_ACE_primary.mp4`

| 字段 | 来源 | 自动获取难度 |
|---|---|---|
| 序号 | 系统自增 | ✅ 自动 |
| 地图 | ❌ 直播无结构化 → 人工标注 / 从赛程页推断 | ⚠️ 半自动 |
| 回合 | ❌ 直播无结构化 → 人工 / 计时推断 | ⚠️ 半自动 |
| 队伍ID | ❌ 画面无 → 人工 / 赛程匹配 | ⚠️ 半自动 |
| 选手ID | ❌ 画面无 → 人工 / 转场字可能含选手名（OCR） | ⚠️ 半自动 |
| 连杀数 | 转场字可推断（ACE=5, 高效火力=多杀） | ⚠️ 半自动 |
| 类型 | 识别词表直接映射 | ✅ 自动 |
| 来源通道 | primary / backup / manual | ✅ 自动 |

**实践建议**：首期文件命名为 `{序号}_{类型}_{UTC时间戳}_{来源}.mp4`（全自动），地图/回合/队伍/选手作为**元数据**存 DB，由运营在后台补填或批量导入赛程映射。完全自动填齐所有字段在当前信号源下不现实，文档需明确此边界。

---

## 7. 风险与边界（必须让落地 agent 知道）

1. **地图/回合/选手ID 无法从直播流可靠自动获取** —— 这是需求里"按规则命名"的最大现实折扣。要么接受半自动（人工补），要么放弃这些字段进文件名。
2. **识别非 100%** —— 漏切靠手动补录、误切靠人工删。需配套"待确认/已录/已删"状态管理。
3. **版权与合规** —— 录制官方直播流切片用于内部二创需注意赛事版权/平台 ToS。建议仅内部使用、不公开分发。
4. **资源成本** —— 常驻 Ingest + 多 Worker + 环形缓冲，需一台常驻机器（本地 Win/Linux 或私有云）。不是 serverless 能跑的。
5. **VOD 延迟** —— 备用通道依赖官方 VOD 上线（通常直播结束后几分钟到几小时），重录非实时。
6. **多路同时开播** —— 若多场比赛同开，需每路独立 Ingest 实例，资源线性增长。

---

## 8. 落地分期建议（给新 agent 的路线图）

- **MVP（2–3 周）**：单路直播接入 + 环形缓冲 + 模板匹配识别 REPLAY/ACE/CLUTCH + 主通道实时切片 + 基础命名 + 本地落盘 + 手动截取按钮（内部小工具形态）。
- **V2**：加 OCR 词表、备用通道（VOD 重切）、任务队列 + 多 worker 并发、卡顿自动转备用、状态管理后台。
- **V3（可选）**：与中台对接（中台只读展示高光库）、批量赛程映射自动填地图/队伍、多路并发。

---

## 9. 给新 agent 的交接要点
- 本系统是**独立后端服务**，语言建议 Python（ffmpeg 生态 + OCR 库成熟）或 Node（若团队偏 JS），与中台解耦。
- 不要尝试接入 Jinx2 中台的代码或 Supabase，除非 V3 明确要做展示对接。
- 核心三件套：**环形缓冲（实时切） + 模板匹配（稳识别） + 任务队列多 worker（不阻塞重录）**。
- 命名规范见 §6，首期接受"半自动填字段"。
- 识别准确率预期见 §4.4，不要承诺全自动零误报。

---

## 10. 实现方式评估与成本（补充评估）

> 本节对 §3/§4 的推荐架构做横向对比，并补充原文档低估的成本与局限，供落地拍板参考。

### 10.1 四种实现路线对比

| 路线 | 做法 | 月度常驻成本 | 实时性 | 准确率 | 最大局限 |
|---|---|---|---|---|---|
| **A 全流录 + 后切** | 整场录盘，赛后检测切高光 | 几乎零 CPU；存储爆（一场 2.5h 1080p ≈ 6GB） | ❌ 非实时 | 取决于检测手段 | 完全不满足"直播中立刻拿到回放" |
| **B 环形缓冲 + 实时检测**（§3 主推） | 常驻写滚动缓冲，抽帧 OCR/模板命中即切 | 本地：电费 ¥10–30；云 VM：¥100–300 | ✅ 准实时 | 模板 85–95% / OCR 60–80% | 需常驻进程、切长难定、模板需逐源采集 |
| **C 路线 B 上云** | 把 B 搬云 VM | ¥100–300（2C4G）；上 GPU ≈ ¥2000–5000 | ✅ | 同 B | 云出站流量费；长期比本地贵 |
| **D VLM 多模态大模型逐帧识别** | 抽帧丢 GPT-4V / 即梦视觉等判定 | 按帧/token 计费，贵且慢 | ❌ 不可实时（秒级延迟） | 高但不可实时 | 只配二次裁定，不宜主通道 |

**省成本关键点**：切片用 ffmpeg `stream copy`（不重新编码）几乎零转码开销，CPU 只花在"抽帧解码做检测"上（约 1 核）。不要把转码当默认步骤，那是白白烧 CPU。

### 10.2 原文档低估 / 漏掉的点

1. **"切多长"根本没解**：§3.1 只说"命中前 X 秒 + 后 Y 秒"固定窗口。但回放转场本身 3–8 秒，高光内容可能更长——固定 Y 会切短或切到死画面。应**同时检测"回放开始"与"回放结束"两个信号**，或给最小/最大窗口 + 末尾静帧检测。
2. **模板匹配被说得太轻松**：文档称"最稳"，但每个源（官方 VCT / 主播二路 / B 站 / 虎牙）转场画面都不同，首期就要为常看的 1–2 路各采一套模板；赛季换包装还要重采。这是**持续人力**，不是一次性。
3. **音频线索被过早否定**：VALORANT 连杀 / ACE 有辨识度极高的游戏内音效。若常看的二路保留游戏原声，音频检测误报可能比 OCR 还低、且极轻量（不解码画面）。值得作为**第三通道**，专门覆盖艺术字 OCR 搞不定的转场。
4. **"备用通道"对很多源不成立**：§5 说断流转备用等 VOD 重切。但大量二路主播 / 非官方源**根本没有可重切的 VOD**——这类源只能靠主通道缓冲，缓冲窗口外素材直接永久丢失。需区分"官方源（有 VOD 可补）"与"非官方源（只靠缓冲）"。
5. **多路成本线性但没量化**：同时 3 场 = 3 份 Ingest + 3 份缓冲 + 3 份检测。本地 4 核机器可能顶不住，得上 8–16 核或云多实例。
6. **命名价值被高估**：地图 / 回合 / 选手 ID 文档自己也说拿不到（§6/§7）。既然大半字段靠人工，首期直接用 `{序号}_{类型}_{时间戳}_{来源}.mp4` + 后台补元数据，别在自动命名上耗工程。

### 10.3 落地拍板建议

- **首期走 B 路线，本地闲置机器**（Win/Linux PC 或 NUC），单路；模板匹配为主 + OCR 为辅 + 音频做可选第三通道；命名用最简全自动格式；手动补录兜底。**月度成本 ≈ 电费 ¥10–30**。
- **别上云 / GPU / VLM 主通道**。VLM 只做"低置信度片段的二次裁定"，替代部分人工确认。
- **MVP 最关键的一步不是写代码，是采模板**：先把常看的那 1–2 路直播转场画面截好存模板库，识别准确率直接决定系统能不能用。
